/*#include <iostream>
#include <math.h>
using namespace std;
double f(double x)
{
	double f;
	double f = (5 - 2 * x * x) / ((1 + x + x * x) / (2 * x + x * x) + 2 - (1 - x - x * x) / (2 * x - x * x));
	return f;
}